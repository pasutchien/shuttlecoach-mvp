/** Barrel export for design-system tokens (SPEC §5). */
export { colors } from './colors';
export type { ColorToken } from './colors';
export { fonts, fontSizes } from './typography';
export { spacing, radii, sizing } from './spacing';
